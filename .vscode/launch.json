{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "run terminal bot",
      "program": "${workspaceRoot}/build/bot.js",
      "args": ["-t"],
      "console": "integratedTerminal",
      "envFile": "${workspaceRoot}/.env"
    }
  ]
}
